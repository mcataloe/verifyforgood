# Frontend Docs Placeholder

This directory is reserved for future frontend-specific documentation surfaces.

Current phase intent:

- make the boundary explicit
- keep frontend docs separate from marketing and portal runtime code
- avoid choosing a docs runtime before the actual requirements exist
