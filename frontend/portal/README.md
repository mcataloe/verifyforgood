# Portal App Shell

This package is the starting point for the authenticated customer portal.

Current phase scope:

- runnable Vite + React + TypeScript shell
- placeholder app boundary only
- shared frontend packages consumed intentionally

Out of scope in this phase:

- authentication flows
- API clients
- billing and settings screens
- route structure beyond the default app shell
